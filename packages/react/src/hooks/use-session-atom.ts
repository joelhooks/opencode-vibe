/**
 * useSessionAtom - React binding to SessionAtom subscription API
 *
 * Re-export of useSession for backward compatibility.
 * Prefer importing useSession directly.
 *
 * @deprecated Use useSession instead
 */

export { useSession as useSessionAtom } from "./use-session.js"
